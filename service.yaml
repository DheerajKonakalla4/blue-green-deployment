apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  type: NodePort
  selector:
    app: my-app
    version: blue
  ports:
    - port: 4000
      targetPort: 4000
      nodePort: 30008